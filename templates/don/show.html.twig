{% extends 'base.html.twig' %}

{% block title %}Don{% endblock %}

{% block body %}
    <h1>Don</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ don.id }}</td>
            </tr>
            <tr>
                <th>Montant_user</th>
                <td>{{ don.montantUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_don_index') }}">back to list</a>

    <a href="{{ path('app_don_edit', {'id': don.id}) }}">edit</a>

    {{ include('don/_delete_form.html.twig') }}
{% endblock %}
